# Secrets -- never commit
.env
.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Smoke-test artefacts: throwaway cheap-model output, never part of the eval set
data/smoke/
results/smoke/

# Local spend ledger (rule 8). Machine-local accounting, not project history.
results/spend_log.jsonl

# Raw upstream dataset (19 MB; fetched by scripts/fetch_dataset.py)
data/raw/

# Phase 4 review-queue database: local SQLite, rebuilt by `make queue-data`
data/queue/*.db

# macOS
.DS_Store
